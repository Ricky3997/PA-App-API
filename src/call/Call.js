import React from 'react';

const Call = (props) => {
        return (
            <div>
                🏋This feature is not ready yet, but I'm working hard on it! 😊
            </div>
        );
};

export default Call;
