import React from 'react';
import NotReadyYet from "../settings/NotReadyYet";

const Call = (props) => {
    return (
        <div>
            <NotReadyYet />
        </div>
    );
};

export default Call;
