import React from 'react';
import NotReadyYet from "../various/NotReadyYet";

const Call = (props) => {
    return (
        <div>
            <NotReadyYet />
        </div>
    );
};

export default Call;
