import React from "react";

const NotReadyYet = (props) => {
  return (
    <div>
      <span role="img" aria-label="gymmer">🏋</span>
      This feature is not ready yet, but we're working hard on it!
      <span role="img" aria-label="humble smile">😊</span>
    </div>
  );
};

export default NotReadyYet;
